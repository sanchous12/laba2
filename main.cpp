#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    cout << "Enter A and B: ";
int a, b, s,k;
cin >> a >> b;
cout << "A + B = " << a + b << '\n'
     << "A - B = " << a - b << '\n'
     << "A * B = " << a * b << '\n'
     << "A / B = " << a / b << '\n';
    return 0;
}
